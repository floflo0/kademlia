syntax = "proto3";

option go_package = "proto/generated";

import "ping.proto";
import "find_node.proto";

message Message {
  bytes kademlia_id = 1;
  oneof payload {
    Ping ping = 2;
    FindNode find_node = 3;
  };
}
